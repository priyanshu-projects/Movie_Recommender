# ── Core ML / data ──────────────────────────────────────────────────────────
# scikit-surprise 1.1.3 does not build on Python 3.14 — install latest from PyPI
scikit-surprise
pandas==2.2.2
numpy>=2.0

# ── Serving ──────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ── MLOps ────────────────────────────────────────────────────────────────────
mlflow==2.13.0
dvc==3.51.2
evidently==0.4.30

# ── Data validation ──────────────────────────────────────────────────────────
# great-expectations does not build on Python 3.14 (mesonpy dep) — using pandera instead
pandera

# ── HTTP / utils ─────────────────────────────────────────────────────────────
requests==2.32.3

# ── Linting / testing (dev only, still included for CI) ──────────────────────
ruff==0.4.9
pytest==8.2.2
